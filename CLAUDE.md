# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트

**마스킹 캠스터디 (가칭: Focus Guardian)** — "AI 감독관이 상주하는 마스킹 캠스터디". 캐릭터로 얼굴을 가려 노출 부담 없이, 혼자여도 AI가 지켜봐 집중이 되고, 예약된 동료와 서로의 집중을 책임지는 온라인 독서실(웹).

기획 근거와 결정의 "왜"는 [기획서.md](기획서.md), 만들 기능의 "무엇을"은 [기능명세-유즈케이스.md](기능명세-유즈케이스.md)에 있다. **코드 작성 전 이 두 문서를 먼저 읽을 것.**

## 현재 상태 & 명령어

- ✅ **선검증 스파이크 통과**: 모바일 브라우저(iPhone 15 Pro Safari)에서 MediaPipe 아바타 마스킹 + 부재/자세 감지가 실사용 FPS로 동작 확인. 발열 최적화 포함 — [spike/index.html](spike/index.html). 이 코드가 비전 파이프라인의 원형이다.
- ✅ Next.js 스캐폴딩 완료 (App Router + TypeScript + Tailwind 4, `src/` 구조, 루트에 위치).
- ⬜ 다음 작업: [기능명세-유즈케이스.md](기능명세-유즈케이스.md) "4. MVP 개발 순서"의 1번(비전 파이프라인 모듈화)부터.

```bash
npm run dev      # 개발 서버 (localhost:3000)
npm run build    # 프로덕션 빌드 (변경 검증에 사용)
npm run lint     # ESLint
```

폰 테스트 방법: `npm run dev` + `cloudflared tunnel --url http://localhost:3000` 로 임시 HTTPS 주소 발급 (카메라 API는 HTTPS 필수).

## 기술 스택 (Web-Native, 계획)

- **Framework**: Next.js (React)
- **Vision / 마스킹**: MediaPipe Face Landmarker — 얼굴 랜드마크·블렌드셰이프로 아바타 구동 + 부재/머리자세/시선 감지
- **AI 감독관(대화)**: WebLLM (브라우저 로컬 LLM) — *P3부터*
- **Speech**: Web Speech API (로컬 음성 합성)
- **Storage**: IndexedDB (세션/로그 로컬 저장)
- **함께 모드 전송**: WebRTC — **영상이 아니라 아바타 모션 데이터**를 전송 (아래 아키텍처 원칙 참고)

## 아키텍처 원칙 (위반하면 제품이 깨진다)

이 프로젝트의 핵심 결정들은 코드만 봐선 드러나지 않으니 반드시 지킬 것:

1. **로컬 우선(local-first)**: 비전·LLM 추론은 전부 사용자 기기에서 수행. 혼자 모드에선 영상이 서버로 나가지 않는다. 서버 컴퓨팅 부담 ≈ 0 설계.
2. **영상이 아니라 모션 데이터를 전송**: 함께 모드에서 동료에게 보내는 건 영상 픽셀이 아니라 아바타를 움직이는 숫자(랜드마크/블렌드셰이프/머리자세). 각 클라이언트가 로컬 렌더링. 이게 프라이버시·UX·인프라비용 세 이득을 동시에 낸다 — **여기에 raw 비디오 스트리밍을 도입하지 말 것.**
3. **마스킹 ↔ 감시의 균형**: 생김새는 숨기되 집중 신호(부재·머리자세·시선)는 살린다. 둘 중 하나로 치우치지 말 것.
4. **오탐 방지는 MVP 핵심 로직**: 감지에는 항상 임계값(threshold) + 시간 지연(delay)을 둬서 스트레칭·잠깐 고개 돌림을 경고하지 않는다. 오탐 1건이 사용자 이탈을 부른다.
5. **AI는 고발자가 아니라 감독관 겸 안전망**: 부적절 행동은 *감지 → 차단/블러 → 퇴장 → 증거 보존 → 사람 검토 → 신고 지원*. 플랫폼이 자동 처벌/소송 주체가 되지 않는다. 미성년자 관련 영상은 별도 법적 의무 영역(함부로 저장/전송 금지).

## 개발 우선순위

- **P1 (MVP)**: "혼자 모드"만으로 완결 — 게스트 체험 → 아바타 선택/캘리브레이션 → 실시간 마스킹 → 부재/자세 감지(오탐방지) → 규칙기반 메시지 → 세션 타이머 → 종료 리포트(로컬). 로그인·룸·소셜은 P1 필수 아님(게스트로 가치 먼저 체험 → 가입 유도).
- **P2**: 함께 모드(예약/매칭 — 자유입장 아님), 안전/모더레이션, 코스메틱 상점.
- **P3+**: WebLLM 대화, 집중도 대시보드, 소셜/랭킹, UGC 마켓.

구현 순서는 [기능명세-유즈케이스.md](기능명세-유즈케이스.md)의 "4. MVP 개발 순서" 참고 (엔진 먼저, UI 나중 — 진행 상황 체크박스 포함).

## 빌드 전 미확정 결정 (기획서 부록)

다음은 아직 열려 있으니, 관련 코드를 짜기 전 사용자에게 확인할 것:
1. 함께 모드에서 원본 영상이 기기를 떠나는가(프라이버시 경계).
2. AI 모더레이션 분석 대상이 원본인가 아바타인가, 동의 흐름은.
3. 미성년자 정책(연령 제한/보호자 동의/닫힌 방 한정).
4. 아바타 마스킹의 가림 정도 vs 남길 신호의 균형점.
